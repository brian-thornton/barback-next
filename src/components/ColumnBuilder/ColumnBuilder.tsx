import styles from "./ColumnBuilder.module.css";

type ColumnBuilderProps = {
  columns?: string[];
};

const ColumnBuilder = ({ columns }: ColumnBuilderProps) => {
  return (
    <div>

    </div>
  );
}

export default ColumnBuilder;